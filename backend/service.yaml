apiVersion: v1
kind: Service

metadata:
  name: be-service
  namespace: backend-ns

spec:
  type: ClusterIP

  selector:
    app: backend
  
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
